var app = {
	name : 'prjApp',
	urlServer: 'http://localhost:1337/',
    // true: llama al servidor, falso: carga datos dummy
    callApi : false,
    // Establece la cantidad de items en las grillas y la cantidad de datos a tomar para paginar
    itemsGrid:10
};